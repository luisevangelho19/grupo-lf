// Lead helpers have been removed for WhatsApp-only flow.
// Keep a tiny stub to avoid accidental imports from old tests.
export const LEADS_DEPRECATED = true

/**
 * NOTE: For production this project should use direct WhatsApp CTAs and
 * client-side deep links. Server-side persistence and forwarding were removed
 * to simplify the stack as requested.
 */
